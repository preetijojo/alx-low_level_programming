#include <stdio.h>
/**
 * main - Entry for the program
 * Return: Always 0 (Success)
 */
int main(void)
{
	int i;
	char ch;

	for (i = 0; i < 16; i++)
	{
		if (i < 10)
		{
		ch = 1 + '0';
	{
		else
		{
			ch = 1 - 10 + 'a';
	}
	putchar(ch);
	}
	putchar('\n');
	return (0);
}
