#include "main.h"
/**
 * _abs - The value of an integer
 * @n: The input number as an integer.
 * Return: Absolute value
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
