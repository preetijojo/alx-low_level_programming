#include "main.h"
#include <stdio.h>
/**
 * mul - check the code
 * @a: multiply with b and returns their product
 * @b: multiply with a and return their product
 * Return: Always 0
 */
int mul(int a, int b)
{
	int a *b;

	printf("(%d)", result);
	return (0);
}
